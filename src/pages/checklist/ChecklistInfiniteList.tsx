import React from 'react';
import { Navigate } from 'react-router-dom';

const ChecklistInfiniteList = () => {
  return <Navigate to="/checklist/mensal" replace />;
};

export default ChecklistInfiniteList;